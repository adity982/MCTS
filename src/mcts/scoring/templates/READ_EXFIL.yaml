id: READ_EXFIL
title: Read then egress attack chain
severity: critical
finding_class: security
min_confidence: 0.6
min_reachability: 0.25
max_depth: 5
exploit_cost: 2
anchor:
  first_edge: READS
layers:
  - dataflow
edge_pattern:
  - kind: READS
    from_kind: tool
  - kind: EGRESS
legacy_finding_id: chain-read-exfil
explanation_steps:
  - "Tool reads sensitive or untrusted data."
  - "Separate tool or handler egresses data to external network."
recommended_fixes:
  - remove_egress_tool
  - scope_read_tools
